# Docker Image for ReScript CI build

Alpine-based Docker image for building statically linked ReScript binaries.

Available in the GitHub Container Registry as `ghcr.io/rescript-lang/rescript-ci-build`.
